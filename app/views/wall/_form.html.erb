<%= form_for @wall, :url=>{:action => 'create', :id =>@wall.id},:remote=>true do |f| %>
  <%= f.hidden_field :user_id, :value=> "#{@user[0].id}" %>
  <%= f.label :comment %>
  <%= f.text_field :post_wall %><br />
  <%= f.submit %>
<% end %>
